version: '3'
services:
  core-sepasang-stg:
    build:
      context: .
      dockerfile: prod.Dockerfile
    container_name: core-sepasang-stg
    restart: always
    ports:
      - 3200:3200
    volumes:
      - .:/app
    env_file:
      - .env
    networks:
      - SJ-NETWORK-CORE  

networks:
  SJ-NETWORK-CORE:
